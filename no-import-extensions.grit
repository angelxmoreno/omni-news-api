language js;

`import $mod from $src` where {
  $src <: r".*\.(?:ts|tsx|js|jsx)['\"]",
  register_diagnostic(
    span = $src,
    message = "Imports must not include file extensions (.js, .ts, .jsx, .tsx).",
    severity = "error"
  )
}